{
	"private": true,
	"name": "todouin",
	"workspaces": [
		"packages/*"
	],
	"scripts": {
		"server:dev": "yarn workspace @todouin/backend dev",
		"server:build": "yarn workspace @todouin/backend start",
		"client:dev": "yarn workspace @todouin/frontend react-scripts start",
		"client:build": "yarn workspace @todouin/frontend react-scripts build"
	}
}
